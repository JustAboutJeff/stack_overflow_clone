<div class="comment">
  <li> <%= comment.content %> </li>
	  <% if current_user && current_user.id == comment.user.id %>
		  <li><%= link_to "Edit Comment", edit_comment_path(comment) %></li>
		  <li><%= link_to "Delete Comment", comment_path(comment), :method => "DELETE"  %></li>
	  <% end %>
	<%= render partial: 'voting/voting', locals: {:element => comment, :type => "comment"} %>
</div>
